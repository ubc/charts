apiVersion: v1
kind: Service
metadata:
  name: {{ include "ipeer.fullname" . }}
  labels:
    {{- include "ipeer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    component: web
    {{- include "ipeer.selectorLabels" . | nindent 4 }}
