apiVersion: v1
kind: Service
metadata:
  name: {{ include "operations-hub.fullname" . }}
  labels:
    {{- include "operations-hub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.app.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "operations-hub.selectorLabels" . | nindent 4 }}
