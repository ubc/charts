{{- if .Values.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "ltic-access-request.fullname" . }}
  labels:
    {{- include "ltic-access-request.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: nginx
  rules:
  - host: {{ replace "http://" "" .Values.ingress.host }}
    http:
      paths:
      - backend:
          service:
            name: {{ template "ltic-access-request.fullname" . }}
            port:
              number: {{ $.Values.service.port }}
        path: /
        pathType: Prefix
      {{- if .Values.mailpit.enabled }}
      - backend:
          service:
            name: {{ template "ltic-access-request.fullname" . }}-mailpit
            port:
              number: {{ $.Values.service.port }}
        path: /mailpit
        pathType: Prefix
      {{- end }}
  {{- if and (hasKey .Values.ingress.annotations "kubernetes.io/tls-acme") (eq (index .Values.ingress.annotations "kubernetes.io/tls-acme") "true") }}
  tls:
    - hosts:
      - {{ replace "http://" "" .Values.ingress.host }}
      secretName: {{ template "ltic-access-request.fullname" . }}-tls
  {{- end }}
{{- end }}
