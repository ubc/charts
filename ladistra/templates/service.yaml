apiVersion: v1
kind: Service
metadata:
  name: {{ include "ladistra.fullname" . }}
  labels:
    {{- include "ladistra.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.app.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "ladistra.selectorLabels" . | nindent 4 }}
